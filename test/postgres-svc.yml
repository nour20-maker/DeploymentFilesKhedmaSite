apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: presprod
spec:
  selector:
    app: postgres
  ports:
    - protocol: TCP
      port: 5432
  clusterIP: None  # Headless service for direct pod access

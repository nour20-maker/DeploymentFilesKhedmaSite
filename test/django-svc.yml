apiVersion: v1
kind: Service
metadata:
  name: khedma-backend
  namespace: presprod
spec:
  selector:
    app: khedma-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP  # You can change this to LoadBalancer or NodePort based on your needs

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cm-acme-http-solver-gfjx5
  namespace: presprod
spec:
  rules:
    - host: khedma123456.ddns.net
      http:
        paths:
          - path: /.well-known/acme-challenge/*
            pathType: Prefix
            backend:
              service:
                name: cm-acme-http-solver-ng5wn  # This is the solver service name
                port:
                  number: 80
